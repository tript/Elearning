<br />

<%= link_to "New Lesson", new_lesson_path, class: "btn btn-primary" %>
<br />
<br />

<table class = "table table-bordered table-striped">
  <thead>.
  <tr>
    <th>Name</th>
    <th>Download Link</th>
    <th> </th>
  </tr>
  </thead>

  <tbody>
  <% @lessons.each do |lesson| %>

      <tr>
        <td><%= lesson.name %></td>
        <td><%= link_to "Download Resume", "#" %></td>
        <td><%= button_to "Delete",  lesson, method: :delete, class: "btn btn-danger", confirm: "Are you sure that you wish to delete #{lesson.name}?" %></td>
      </tr>

  <% end %>
  </tbody>

</table>